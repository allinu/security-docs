---
title: 网络安全漏洞资讯日报
date: 2026-08-15
description: 2026年08月15日网络安全漏洞、公开PoC、在野利用及修复信息汇总
tags:
  - 漏洞情报
  - CVE
  - 网络安全日报
---

# 网络安全漏洞资讯日报

> **报告日期：** 2026年08月15日  
> **报告期次：** 2026年第15期  
> **统计周期：** 2026-08-15 00:00 至 2026-08-15 24:00（Asia/Shanghai，目标日期模式）  
> **情报等级：** 重要  
> **编制单位：** 网络安全漏洞情报站（docs.allinu.eu.org）  
> **数据来源：** NVD、CISA、CNVD、CNNVD、厂商公告、GitHub及公开威胁情报平台

> **采集说明：** 本次为目标日期模式任务（存在 `.target_date` 文件，内容 2026-08-15），用于补生成缺失的 2026-08-15 期日报。预抓取脚本于 2026-08-17 01:00 UTC（北京时间 09:00）完成 48 小时窗口抓取（20 个订阅源全部成功，候选条目 23 条），按目标日期 2026-08-15 00:00–24:00（Asia/Shanghai，即 2026-08-14T16:00Z–2026-08-15T16:00Z）过滤后，窗口内候选 8 条，命中网络安全相关条目 6 条（SecWiki 当日无更新内容、Windows 11 质量更新动态 2 条剔除）。本期收录漏洞 3 个（含 1 个暂无 CVE 编号的 GeoServer 零日，以奇安信 `QVD-2026-52627` / GitHub `GHSA-mqjf-5f49-2fjh` 编号），**3 个均为严重（CVSS 9.8~10.0），其中 2 个已确认在野利用、1 个已观测到利用尝试**：`CVE-2026-58231`（SAP Commerce Cloud Data Hub Adapter 未授权 RCE，CVSS 10.0，SAP 补丁发布约 3 天后 Defused 蜜罐即观测到利用尝试）、`CVE-2026-65400`（macOS Screen Sharing 认证绕过，CVSS 9.8，荷兰 NCSC 确认在野利用，攻击者获取 root 并植入门罗币挖矿程序）、`QVD-2026-52627`/`GHSA-mqjf-5f49-2fjh`（GeoServer jsonArrayContains 未授权 SQL 注入，CVSS 9.8，watchTowr 观测到公开后数小时内即遭数百次探测）。**特别说明：** 2026-08-16 期日报生成时因 08-15 期缺失已提前补充收录上述重大通告，本期为按目标日期机制补生成的原始 08-15 期记录，与 08-16 期内容存在交叉属预期。CVE 编号、CWE 及 CVSS 评分经 [NVD API](https://services.nvd.nist.gov/rest/json/cves/2.0) 核验（查询时间 2026-08-17）；发生时间经 web-search-skill（百度/必应）与官方来源交叉核实（`CVE-2026-58231`、`CVE-2026-65400` 及 `QVD-2026-52627`）；PoC 经 GitHub API 仓库搜索与公开网络检索核实；未披露信息如实标注"未披露/未知"，严禁编造。

---

## 一、今日漏洞态势

### 1.1 漏洞统计

| 指标 | 数量 | 指标 | 数量 |
| --- | ---: | --- | ---: |
| 收录漏洞总数 | 3 | 严重漏洞 | 3 |
| 高危漏洞 | 0 | 中危漏洞 | 0 |
| 低危漏洞 | 0 | 公开PoC | 2 |
| 公开利用代码 | 1 | 在野利用 | 3 |
| CISA KEV | 0（本期新增） | 已发布补丁 | 3 |
| 暂无补丁 | 0 | 需紧急处置 | 3 |

> 注：本期 3 个漏洞分布为——SAP Commerce Cloud 1 个（`CVE-2026-58231`，CVSS 10.0）、Apple macOS 1 个（`CVE-2026-65400`，CVSS 9.8）、GeoServer 1 个（`QVD-2026-52627`/GHSA-mqjf-5f49-2fjh，CVSS 9.8，暂无 CVE 编号）。"公开PoC"计 2（`CVE-2026-65400` 研究社区发布混淆 PoC 二进制、`QVD-2026-52627` GitHub Python PoC 仓库）；"公开利用代码"计 1（`CVE-2026-65400` 公开利用代码已被确认用于在野攻击）；"在野利用"计 3（`CVE-2026-58231` Defused 蜜罐确认、`CVE-2026-65400` 荷兰 NCSC-NL 确认、`QVD-2026-52627` watchTowr 观测到利用尝试/探测）；"需紧急处置"按 P0/P1 计 3（`CVE-2026-58231/65400` 为 P0，`QVD-2026-52627` 为 P1）。3 个漏洞均已发布官方补丁或修复版本。

### 1.2 风险等级分布

| 风险等级 | CVSS范围 | 数量 | 占比 |
| --- | ---: | ---: | ---: |
| 严重 | 9.0–10.0 | 3 | 100.0% |
| 高危 | 7.0–8.9 | 0 | 0% |
| 中危 | 4.0–6.9 | 0 | 0% |
| 低危 | 0.1–3.9 | 0 | 0% |

> 注：严重 3 个为 `CVE-2026-58231`（10.0）、`CVE-2026-65400`（9.8）、`QVD-2026-52627`/GHSA-mqjf-5f49-2fjh（9.8，GHSA 口径）。本期无中低危 CVE 收录。

### 1.3 今日重点关注

1. **`CVE-2026-58231`：SAP Commerce Cloud Data Hub Adapter 未授权远程代码执行漏洞（CVSS 10.0，在野利用）**  
   未认证远程攻击者可通过访问 Data Hub Adapter 的导入端点 `/datahubadapter/import/**`，滥用默认认证客户端提交特制数据触发任意代码执行。SAP 于 2026-08-11 补丁日发布修复（Security Note 3771065），威胁情报公司 Defused Cyber 蜜罐在补丁发布约 3 天后即观测到利用尝试（典型的补丁对比攻击）。当前状态为 **补丁已发布（Note 3771065，需升级至 2211.55 / 2211-jdk21.17）**、**公开 PoC 未发现**、**在野利用已确认**，处置优先级 **P0**。

2. **`CVE-2026-65400`：macOS Screen Sharing 认证绕过漏洞（CVSS 9.8，在野利用，公开利用代码）**  
   `screensharingd` 的 SRP 帧长度校验存在认证状态管理缺陷，网络攻击者可无需凭证认证到 Screen Sharing 服务，并借助 root 权限的文件助手进程实现文件读取乃至远程代码执行。荷兰 NCSC 确认在野攻击中攻击者获取 **root 权限并植入门罗币挖矿程序**（针对互联网暴露 5900 端口的 Mac）。当前状态为 **补丁已发布（macOS Tahoe 26.6.1 / Sequoia 15.7.9 / Sonoma 14.8.9，2026-08-06）**、**公开利用代码已出现**、**在野利用已确认**，处置优先级 **P0**。

3. **`QVD-2026-52627` / `GHSA-mqjf-5f49-2fjh`：GeoServer jsonArrayContains 未授权 SQL 注入漏洞（CVSS 9.8）**  
   2026-08-12 由研究者 @q1uf3ng 公开的 GeoServer 零日，OGC Filters 配合 PostGIS DataStore 的 `jsonArrayContains` 函数将 `<value>` 未转义拼入 SQL，未授权攻击者可注入 SQL，数据库为高权限（sa）账号时可实现远程代码执行。watchTowr 在公开后数小时内即观测到数百次探测尝试；该漏洞为 `CVE-2023-25158` 的回归。GeoServer 已发布修复版本 **3.0.1 / 2.28.5 / 2.27.6**。当前状态为 **修复版本已发布**、**公开 PoC 已出现**、**已观测利用尝试（探测为主）**，处置优先级 **P1**。

### 1.4 综合研判

本期为"在野利用密集"态势：收录的 3 个严重漏洞均已存在在野利用或利用尝试记录。SAP Commerce Cloud 与 macOS Screen Sharing 两个漏洞在补丁发布后极短时间内即被武器化利用（前者无公开 PoC 仍遭利用，属补丁对比攻击；后者公开利用代码直接用于在野挖矿攻击），GeoServer 零日公开数小时内即遭批量探测且已有公开 PoC 仓库。三者均面向互联网暴露面（SAP Data Hub 导入端点、macOS 5900 端口、GeoServer OGC 服务），且相关产品在企业与互联网环境部署广泛，建议按 P0/P1 优先级立即开展资产排查与补丁升级。

!!! danger "研判结论：较高风险"
    今日核心风险点为 SAP Commerce Cloud（CVSS 10.0、在野利用）、macOS Screen Sharing（在野利用、root 权限、公开利用代码）与 GeoServer（公开 PoC、批量探测）三线并进，且均面向互联网暴露面。要求今日内完成受影响资产清点，P0 项 24 小时内完成补丁升级或断网/收敛访问，P1 项 3 个工作日内处置完毕。

---

## 二、重点漏洞清单

| 序号 | CVE编号 | 漏洞名称 | 影响产品 | 等级 | CVSS | PoC | 在野利用 | 修复状态 |
| --- | --- | --- | --- | --- | ---: | --- | --- | --- |
| 1 | `CVE-2026-58231` | SAP Commerce Cloud Data Hub Adapter 未授权 RCE | SAP Commerce Cloud | 严重 | 10.0 | 未公开 | 是（已确认） | 已发布补丁 |
| 2 | `CVE-2026-65400` | macOS Screen Sharing 认证绕过（预认证访问/文件读取） | Apple macOS | 严重 | 9.8 | 已公开 | 是（已确认） | 已发布补丁 |
| 3 | `QVD-2026-52627`（GHSA-mqjf-5f49-2fjh） | GeoServer jsonArrayContains 未授权 SQL 注入 | GeoServer / GeoTools | 严重 | 9.8 | 已公开 | 是（已观测利用尝试） | 已发布补丁 |

---

## 三、漏洞详情

<!-- 层级规范：三/四/五/六 为 H2（##），每个漏洞为 H3（### 3.1、3.2…），漏洞内小节为 H4（#### 3.N.1、3.N.2…），确保右侧目录可逐级跳转 -->

### 3.1 CVE-2026-58231 — SAP Commerce Cloud Data Hub Adapter 未授权远程代码执行漏洞（在野利用）

!!! danger "严重漏洞"
    SAP Commerce Cloud 的 Data Hub Adapter 组件存在授权检查不足与输入验证缺失，未认证远程攻击者访问互联网暴露的导入端点即可提交特制数据，可能导致任意代码执行并危及内部组件。

#### 3.1.1 基本信息

| 字段 | 内容 |
| --- | --- |
| **CVE编号** | `CVE-2026-58231` |
| **CWE编号** | `CWE-94`（Code Injection；NVD/SAP CNA 标注）；漏洞成因同时涉及授权检查不足（与 `CWE-284/285` 相关） |
| **漏洞名称** | SAP Commerce Cloud Data Hub Adapter 未授权远程代码执行漏洞 |
| **漏洞类型** | 远程代码执行（RCE，未授权） |
| **风险等级** | 严重 |
| **CVSS评分** | 10.0 |
| **CVSS向量** | `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H` |
| **EPSS评分** | 暂无数据（NVD 收录时间较短，FIRST EPSS 尚未收录该 CVE） |
| **披露日期** | 2026-08-11（SAP 2026 年 8 月安全补丁日；NVD 收录时间 2026-08-11） |
| **更新日期** | 2026-08-12（NVD） |
| **CISA KEV** | 否（截至 2026-08-15 未收录） |
| **在野利用** | 已确认（Defused Cyber 蜜罐观测到利用尝试，SAP 补丁发布约 3 天后） |
| **PoC状态** | 未公开（Defused 明确指出当时无公开 PoC） |
| **补丁状态** | 已发布（SAP Security Note 3771065） |
| **信息可信度** | 高（NVD/厂商/多家安全媒体交叉印证） |

#### 3.1.2 漏洞摘要

该漏洞存在于 SAP Commerce Cloud（原 Hybris）的 **Data Hub Adapter** 组件，影响 COM_CLOUD 2211 与 2211-JDK21 版本线。适配器的数据导入端点 `/datahubadapter/import/**` 在接收数据时未正确校验调用方身份，未认证远程攻击者可直接访问该端点并提交精心构造的数据，适配器将其作为可信输入处理，最终可能导致任意代码执行，并进一步危害与 Commerce Cloud 集成的内部组件。

SAP 官方描述为：未认证攻击者可滥用默认认证客户端，向缺乏充分校验的特定函数提交特制输入，成功利用可造成任意代码执行，对应用的机密性、完整性与可用性产生高影响。威胁情报公司 Defused Cyber 披露，针对该漏洞的利用尝试在补丁发布后仅 3 天即命中其蜜罐系统，且当时没有公开 PoC、此前也未发现在野利用——攻击者很可能在补丁披露后通过逆向补丁自行开发了利用工具，属典型的补丁对比（patch diffing）攻击手法。

> 该漏洞为授权检查不足（Improper Authorization）叠加输入验证缺失的典型组合：端点本身缺少身份校验，输入数据又缺少消毒处理，二者叠加形成未认证 RCE。

#### 3.1.3 影响产品与版本

| 厂商 | 产品/组件 | 受影响版本 | 修复版本 | 部署形态 |
|---|---|---|---|---|
| SAP | Commerce Cloud（COM_CLOUD）Data Hub Adapter | `2211`、`2211-JDK21`（使用默认认证客户端的版本均可能受影响） | `2211.55`、`2211-jdk21.17` 或更高版本 | 云服务/自托管 |

!!! warning "版本说明"
    版本范围以 SAP 官方安全公告（Security Note 3771065）为准。SAP 未公布完整受影响版本清单时，OpenCVE 等第三方提示"所有使用默认认证客户端的发布版本均可能受影响"，具体以厂商公告为准。

#### 3.1.4 漏洞分类

| 分类体系 | 编号 | 名称 |
| --- | --- | --- |
| CWE | `CWE-94` | 代码注入（Code Injection） |
| OWASP | `A03:2021` | 注入（Injection） |
| CAPEC | `CAPEC-242` | 代码注入（Code Injection） |

#### 3.1.5 利用条件与技术原理

| 条件 | 状态 | 说明 |
| --- | --- | --- |
| 网络可达 | 是 | 需可访问 Data Hub Adapter 导入端点（`/datahubadapter/import/**`） |
| 身份认证 | 无需 | 未认证即可访问，可滥用默认认证客户端 |
| 用户交互 | 不需要 | 无需点击链接或打开文件 |
| 特定配置 | 是 | 使用默认认证客户端的 Commerce Cloud 环境受影响 |
| 利用复杂度 | 低 | 构造特制请求即可，无需前置条件 |

**技术原理：**

Data Hub Adapter 的导入端点缺少对调用方身份的充分授权校验（CWE-284 相关），同时提交的数据缺乏输入验证与消毒（CWE-94 相关），导致攻击者提交的恶意数据被当作可信输入处理，触发任意代码执行。

**核心利用链：**

```text
攻击者
  └─ 发现暴露的 Data Hub Adapter 导入端点 /datahubadapter/import/**
      └─ 构造恶意导入数据（无需身份认证）
          └─ 滥用默认认证客户端提交至缺乏校验的函数
              └─ 数据被当作可信输入处理
                  └─ 触发任意代码执行，危及内部组件
```

#### 3.1.6 MITRE ATT&CK 映射

| 战术 | 技术编号 | 技术名称 | 关联说明 |
| --- | --- | --- | --- |
| Initial Access | `T1190` | Exploit Public-Facing Application | 利用互联网暴露的 Data Hub 导入端点获取初始访问 |

> 仅保留与实际利用场景相关的 ATT&CK 技术，不机械填充完整攻击链。

#### 3.1.7 PoC与利用情况

| 字段 | 内容 |
| --- | --- |
| **PoC状态** | 未公开（Defused Cyber 确认公开披露时无公开 PoC） |
| **PoC来源** | 不适用（未发现公开 PoC） |
| **PoC地址** | 不适用 |
| **作者/组织** | 不适用 |
| **发布日期** | 不适用 |
| **支持版本** | 不适用 |
| **验证方式** | 无害化验证：仅核对 SAP Commerce Cloud 版本是否 ≥ 2211.55 / 2211-jdk21.17，或核对 Data Hub 导入端点是否配置 IP Filter Set |
| **代码可信度** | 不适用（无公开 PoC） |
| **安全审计情况** | 不适用 |

**PoC能力：**

- [x] 仅识别版本
- [ ] 可无害验证漏洞
- [ ] 可读取目标信息
- [ ] 可执行命令
- [ ] 可获取目标权限
- [ ] 存在破坏性或持久化行为
- [ ] 存在可疑外联或供应链风险

???+ warning "PoC代码或验证请求"

    未发现公开 PoC（web-search-skill 百度/必应检索与 GitHub API 仓库搜索均无结果）。以下为基于 SAP 公告与公开技术分析整理的**无害化核查思路**（仅用于防护与核查，不构成利用载荷）：

    ```text
    # 1. 版本核查（无害）
    #    确认 SAP Commerce Cloud 版本是否达到修复版本 2211.55 / 2211-jdk21.17
    # 2. 暴露面核查（仅查看自身资产配置）
    #    确认 Data Hub Adapter 导入端点 /datahubadapter/import/** 是否仅允许可信 Data Hub 服务器地址访问
    # 3. 厂商临时缓解（SAP 官方建议）
    #    配置 IP Filter Set，将导入端点访问限制为可信 Data Hub 服务器地址
    ```

#### 3.1.8 暴露面与资产检索

| 类型 | 检索特征 |
| --- | --- |
| HTTP标题 | `title="SAP Commerce"`（公开语法示例） |
| 响应头 | `Server: SAP`（公开语法示例） |
| 页面特征 | `body="/datahubadapter/"`（公开语法示例） |
| Favicon Hash | 暂无公开已知哈希 |
| 默认端口 | `443`（HTTPS 管理/导入端点） |
| FOFA | `body="/datahubadapter/import"`（公开语法示例） |
| Hunter | `web.body="/datahubadapter/"`（公开语法示例） |
| Shodan | `http.html:"datahubadapter"`（公开语法示例） |
| ZoomEye | `app:"SAP Commerce Cloud"`（公开语法示例） |

> **未执行任何查询。** 本栏目仅列出公开已知的检索语法示例，严禁对目标执行主动扫描或调用测绘平台。

#### 3.1.9 检测与排查

#### 资产排查

- [ ] 是否部署受影响产品或组件
- [ ] 当前版本是否处于影响范围
- [ ] 服务或管理接口是否暴露互联网
- [ ] 漏洞相关功能是否启用
- [ ] 是否存在默认账号、弱口令或默认配置
- [ ] 是否已安装官方补丁或采取缓解措施

#### 日志与行为排查

重点检查：

- Web访问日志、应用日志和系统安全日志；
- WAF、防火墙、IDS/IPS及态势感知告警；
- Data Hub Adapter 导入端点访问记录及异常导入请求；
- Web服务异常拉起Shell、PowerShell、curl、wget等进程；
- 临时目录、Web目录和启动目录中的可疑文件；
- 异常DNS解析、外联IP、反向连接及数据传输行为。

| 检测项 | 可疑特征 |
| --- | --- |
| 请求路径 | `/datahubadapter/import/**` 的非预期来源访问 |
| 请求方法 | `GET/POST`（导入端点异常调用） |
| 参数特征 | 数据导入载荷中包含命令、表达式或编码混淆特征 |
| User-Agent | 非标准 UA 或自动化工具特征 |
| 响应状态 | 导入端点返回异常状态码或错误信息 |
| 进程行为 | Web服务拉起命令解释器或下载工具 |
| 文件行为 | 写入临时目录、Web目录或启动目录 |
| 网络行为 | 异常DNS、恶意外联或反向连接 |

#### 3.1.10 IOC与检测规则

#### IOC信息

| 类型 | IOC | 说明 | 可信度 |
| --- | --- | --- | --- |
| IP | 暂无公开 IOC | Defused Cyber 蜜罐观测未公布来源 IP | 低 |
| 域名 | 暂无公开 IOC | — | 低 |
| URL | `/datahubadapter/import/**` | 被利用端点（非攻击者 IOC，属排查特征） | 高 |
| SHA256 | 暂无公开 IOC | — | 低 |

???+ example "Sigma / YARA / Snort / Suricata规则"

    ```text
    # 暂无公开检测规则。建议在 WAF/IPS 侧对 /datahubadapter/import/** 端点
    # 的非白名单来源访问进行告警与限速，并结合应用日志排查异常导入请求。
    ```

#### 3.1.11 风险影响

漏洞成功利用后，可能导致：

- 未经授权访问系统或业务接口；
- 远程执行任意系统命令；
- 获取应用、系统或数据库权限；
- 泄露账号、密钥、配置文件及业务数据；
- 篡改网站页面、配置或业务数据；
- 植入WebShell、后门或恶意程序；
- 横向移动至其他服务器或业务系统；
- 中断业务服务或破坏系统可用性；
- 被勒索软件、挖矿木马或僵尸网络利用。

#### 3.1.12 处置优先级

| 研判维度 | 状态 |
| --- | --- |
| 互联网暴露 | 是 |
| 无需认证利用 | 是 |
| 利用复杂度低 | 是 |
| 已公开PoC | 否 |
| 已确认在野利用 | 是 |
| 已纳入CISA KEV | 否 |
| 影响核心业务 | 是 |
| 涉及敏感数据 | 是 |

!!! danger "综合处置优先级：P0"
    - **P0：紧急处置**，已在野利用或可直接影响核心互联网资产；
    - **P1：24小时内处置**，PoC公开且利用门槛较低；
    - **P2：3个工作日内处置**，利用条件受限或暂无成熟PoC；
    - **P3：常规整改**，风险较低或仅影响非关键场景。

#### 3.1.13 修复与缓解措施

#### 官方修复

1. 将 SAP Commerce Cloud 升级至 `2211.55` / `2211-jdk21.17` 或更高版本；
2. 安装 SAP 官方安全补丁（Security Note `3771065`）；
3. 升级前完成配置、程序和业务数据备份；
4. 在测试环境验证兼容性及回退方案；
5. 升级后核验版本、补丁状态及业务运行情况；
6. 重新执行漏洞检测并留存修复证据。

#### 临时缓解

无法立即升级时，可采取：

- 按 SAP Note 3771065 配置 **IP Filter Set**，将 Data Hub Adapter 导入端点访问限制为可信 Data Hub 服务器地址；
- 关闭或禁用不受业务必需的导入端点/功能；
- 限制相关端口和管理接口的互联网访问；
- 通过防火墙、ACL、安全组仅允许可信IP访问；
- 在WAF、IPS或网关增加针对性拦截规则；
- 启用身份认证、多因素认证和最小权限控制；
- 修改默认账号、默认密码及默认密钥；
- 加强日志审计、主机监控和异常外联检测；
- 对高风险系统实施网络隔离或访问收敛。

#### 3.1.14 修复验证

- [ ] 产品已升级至安全版本
- [ ] 官方补丁已正确安装
- [ ] 高风险接口已限制访问
- [ ] PoC或检测规则无法再次触发
- [ ] 未发现异常账号、进程、文件或计划任务
- [ ] 未发现异常网络连接或数据外传
- [ ] 业务功能运行正常
- [ ] 修复过程及验证结果已留档

#### 3.1.15 本地资产关联情况

| 项目 | 结果 |
| --- | --- |
| 是否存在相关资产 | 待核查 |
| 关联资产数量 | 不适用 |
| 互联网暴露数量 | 不适用 |
| 确认受影响数量 | 不适用 |
| 已修复数量 | 不适用 |
| 待修复数量 | 不适用 |
| 责任单位 | 不适用 |
| 完成时限 | 不适用 |
| 当前状态 | 待核查 |

> 本站在线公开文档、无内部资产台账，本地资产关联情况不适用，需各单位结合自身资产清单核查。

#### 3.1.16 参考资料

1. [SAP Security Note 3771065](https://me.sap.com/notes/3771065)
2. [SAP 安全补丁日](https://url.sap/sapsecuritypatchday)
3. [NVD - CVE-2026-58231](https://nvd.nist.gov/vuln/detail/CVE-2026-58231)
4. [CVE.org - CVE-2026-58231](https://www.cve.org/CVERecord?id=CVE-2026-58231)
5. [安全圈：SAP云端漏洞评分满分，补丁发布3天就被利用](https://mp.weixin.qq.com/s?__biz=MzIzMzE4NDU1OQ==&mid=2652078290&idx=3&sn=197f827e1231607636b09c2de8831b96)
6. [Security Affairs：SAP Commerce Cloud CVE-2026-58231 Exploited in the Wild](https://securityaffairs.com/197244/security/sap-commerce-cloud-cve-2026-58231-exploited-in-the-wild.html)

---

### 3.2 CVE-2026-65400 — macOS Screen Sharing 认证绕过漏洞（在野利用，公开利用代码）

!!! danger "严重漏洞"
    macOS 内置 Screen Sharing 服务（screensharingd）存在认证状态管理缺陷，网络攻击者无需有效凭证即可认证登录，并可借助 root 权限的文件助手进程读取任意文件乃至实现远程代码执行；荷兰 NCSC 已确认在野利用。

#### 3.2.1 基本信息

| 字段 | 内容 |
| --- | --- |
| **CVE编号** | `CVE-2026-65400` |
| **CWE编号** | `CWE-287`（Improper Authentication） |
| **漏洞名称** | macOS Screen Sharing 认证绕过漏洞（预认证访问） |
| **漏洞类型** | 认证绕过（Authentication Bypass）→ 预认证文件读取/远程代码执行 |
| **风险等级** | 严重 |
| **CVSS评分** | 9.8 |
| **CVSS向量** | `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H` |
| **EPSS评分** | 暂无数据（FIRST EPSS 尚未收录） |
| **披露日期** | 2026-08-06（Apple 安全更新发布并披露；NVD 收录 2026-08-06） |
| **更新日期** | 2026-08-15（NVD） |
| **CISA KEV** | 否（截至 2026-08-15 未收录） |
| **在野利用** | 已确认（荷兰 NCSC-NL：针对互联网暴露 5900 端口的 Mac，获取 root 权限并植入门罗币挖矿程序） |
| **PoC状态** | 已公开（研究社区发布混淆 PoC 二进制；NCSC 指出公开利用代码出现后被用于在野攻击） |
| **补丁状态** | 已发布（macOS Tahoe 26.6.1 / Sequoia 15.7.9 / Sonoma 14.8.9，2026-08-06） |
| **信息可信度** | 高（Apple/NVD/NCSC/多家安全厂商交叉印证） |

#### 3.2.2 漏洞摘要

该漏洞位于 macOS 内置远程桌面功能 Screen Sharing（基于 VNC/RFB 协议，监听 TCP 5900 端口）的系统守护进程 `screensharingd`。Apple 官方描述为"通过改进状态管理修复了认证问题，网络攻击者可在没有有效凭证的情况下认证到 Screen Sharing"。安全研究者（Huntress/fG!/blasty）进一步指出，根因是 `screensharingd` 的 **SRP（Secure Remote Password）帧长度校验缺失错误返回**，属逻辑缺陷而非内存破坏：攻击者按特定顺序重放握手即可被当作已登录用户。

Screen Sharing 服务由两个以高权限运行的文件助手进程 `SSFileCopySender` / `SSFileCopyReceiver` 支撑文件操作，二者继承 Apple 签名授权 `kTCCServiceSystemPolicyAllFiles`（全磁盘访问权限，可绕过 TCC 防护）。利用该认证绕过可触发文件复制功能读取任意文件，在部分条件下可实现预认证远程代码执行。荷兰 NCSC-NL 确认的在野攻击中，攻击者获取 **root 权限** 并植入门罗币（Monero）挖矿程序。

> 该漏洞无需任何凭证、无需用户交互，仅需目标 Mac 的 Screen Sharing 对攻击者网络可达。研究者 @osxreverser 扫描估计互联网上暴露 Screen Sharing 的 Mac 约 **4 万台**（近半数位于美国），覆盖家庭、高校与企业服务器。

#### 3.2.3 影响产品与版本

| 厂商 | 产品/组件 | 受影响版本 | 修复版本 | 部署形态 |
|---|---|---|---|---|
| Apple | macOS Screen Sharing（screensharingd） | `macOS Sonoma 14.0–14.8.8`、`macOS Sequoia 15.0–15.7.8`、`macOS Tahoe 26.0–26.6.0` | `macOS Sonoma 14.8.9`、`Sequoia 15.7.9`、`Tahoe 26.6.1` | 操作系统内置服务（TCP 5900） |

!!! warning "版本说明"
    版本范围以 Apple 官方安全公告（APPLE-SA-08-06-2026-2/3，support.apple.com/en-us/148170/148171/148172）与 NVD CPE 范围为准。Apple 另于 2026-07-27 发布一批更新，部分修复了 `screensharingd` 预认证问题，完整修复以 2026-08-06 版本为准。

#### 3.2.4 漏洞分类

| 分类体系 | 编号 | 名称 |
| --- | --- | --- |
| CWE | `CWE-287` | 认证不当（Improper Authentication） |
| OWASP | `A07:2021` | 身份识别与认证失败（Identification and Authentication Failures） |
| CAPEC | `CAPEC-115` | 认证绕过（Authentication Bypass） |

#### 3.2.5 利用条件与技术原理

| 条件 | 状态 | 说明 |
| --- | --- | --- |
| 网络可达 | 是 | 目标 5900 端口（Screen Sharing/VNC）对攻击者网络可达 |
| 身份认证 | 无需 | 认证绕过，无需任何账号凭证 |
| 用户交互 | 不需要 | 无需点击链接或打开文件 |
| 特定配置 | 是 | Screen Sharing 功能启用且可被网络访问（互联网暴露即高危） |
| 利用复杂度 | 低 | 逻辑缺陷，无需内存破坏利用链 |

**技术原理：**

`screensharingd` 在 SRP 认证帧长度校验中缺少错误返回（missing error-return），导致认证状态管理缺陷：攻击者可构造异常握手序列，使服务在未提供有效凭证的情况下将连接标记为已认证。认证通过后，攻击者可调用以 root 权限运行、持有全磁盘访问授权（`kTCCServiceSystemPolicyAllFiles`）的 `SSFileCopySender`/`SSFileCopyReceiver` 文件助手，读取系统任意文件；结合文件写入能力可进一步实现远程代码执行。

**核心利用链：**

```text
攻击者（网络可达 5900 端口）
  └─ 构造异常的 SRP 认证握手帧序列
      └─ 触发 screensharingd 帧长度校验缺失错误返回
          └─ 绕过凭证校验，被标记为已认证
              └─ 调用 root 权限文件助手（SSFileCopySender/Receiver）
                  └─ 读取任意文件（绕过 TCC）/ 写入文件
                      └─ 获取 root 权限并植入挖矿程序等载荷
```

#### 3.2.6 MITRE ATT&CK 映射

| 战术 | 技术编号 | 技术名称 | 关联说明 |
| --- | --- | --- | --- |
| Initial Access | `T1190` | Exploit Public-Facing Application | 利用互联网暴露的 Screen Sharing 服务获取初始访问 |
| Initial Access | `T1210` | Exploit Remote Service | 利用远程服务（5900/VNC）的认证绕过漏洞 |

> 仅保留与实际利用场景相关的 ATT&CK 技术，不机械填充完整攻击链。

#### 3.2.7 PoC与利用情况

| 字段 | 内容 |
| --- | --- |
| **PoC状态** | 已公开（公开利用代码已出现；NCSC 指出公开利用代码出现后即被用于在野攻击） |
| **PoC来源** | 研究社区公开（Pedro Vilaça/fG! 发布的混淆 PoC 二进制及技术博客；blasty 技术分析）；GitHub 仓库搜索未发现以该 CVE 命名的 PoC 仓库 |
| **PoC地址** | 未在 GitHub 检索到仓库；技术分析见 [Huntress 博客](https://www.huntress.com/blog/macos-screen-sharing-rce-patched) 与 [seclists Full Disclosure](http://seclists.org/fulldisclosure/2026/Aug/37) |
| **作者/组织** | Pedro Vilaça（fG!）、blasty、Huntress（Ryan Dowd 等） |
| **发布日期** | 2026-07-29 ~ 2026-08-07（fG!/blasty 披露 CVE-2026-65400 分析及混淆 PoC） |
| **支持版本** | macOS ≤ 26.5.2 / ≤ 15.7.7 / ≤ 14.8.7（未打 2026-07-27/08-06 补丁的版本） |
| **验证方式** | 无害化验证：仅核对 macOS 版本与 Screen Sharing 启用状态（系统设置 → 通用 → 共享 → 屏幕共享）；PoC 能力为预认证文件读取/代码执行 |
| **代码可信度** | 低（fG! 发布的 PoC 为混淆二进制，未审计，存在供应链风险，严禁在非隔离环境运行） |
| **安全审计情况** | 混淆二进制未审计；Huntress/fG! 技术分析可信度较高 |

**PoC能力：**

- [ ] 仅识别版本
- [x] 可无害验证漏洞
- [x] 可读取目标信息（任意文件读取）
- [x] 可执行命令
- [x] 可获取目标权限（root）
- [ ] 存在破坏性或持久化行为
- [x] 存在可疑外联或供应链风险（混淆二进制，未审计）

???+ warning "PoC代码或验证请求"

    公开 PoC 为**混淆二进制**（fG! 发布，未审计，存在供应链风险），本报告**不嵌入、不推荐运行**。以下仅为基于公开技术分析的**无害化核查建议**：

    ```text
    # 1. 版本核查（无害）
    #    system_profiler SPSoftwareDataType | grep "System Version"
    #    确认是否已升级到 macOS Tahoe 26.6.1 / Sequoia 15.7.9 / Sonoma 14.8.9
    # 2. 暴露面核查（本机配置）
    #    系统设置 → 通用 → 共享 → 屏幕共享：确认是否启用
    #    lsof -iTCP:5900 -sTCP:LISTEN -n -P   # 查看 5900 端口监听与防火墙状态
    # 3. 临时缓解
    #    关闭屏幕共享；或通过防火墙仅允许可信来源访问 5900 端口
    ```

    技术原理示意（源自 Huntress/fG!/blasty 公开分析，非可利用载荷）：`screensharingd` SRP 帧长度校验缺失错误返回 → 认证状态被错误标记为已登录。禁止对未授权目标执行任何验证。

#### 3.2.8 暴露面与资产检索

| 类型 | 检索特征 |
| --- | --- |
| HTTP标题 | 不适用（VNC/RFB 非 HTTP 服务） |
| 响应头 | 不适用（VNC/RFB 非 HTTP 服务） |
| 页面特征 | 不适用（VNC/RFB 非 HTTP 服务） |
| Favicon Hash | 不适用 |
| 默认端口 | `5900`（VNC/RFB，Screen Sharing） |
| FOFA | `port="5900" && protocol="vnc"`（公开语法示例） |
| Hunter | `port="5900"`（公开语法示例） |
| Shodan | `port:5900 product:"Apple Mac OS X Remote Management"`（公开语法示例） |
| ZoomEye | `port:"5900"`（公开语法示例） |

> **未执行任何查询。** 本栏目仅列出公开已知的检索语法示例，严禁对目标执行主动扫描或调用测绘平台；研究者 @osxreverser 的公开扫描估计约 4 万台 Mac 暴露，未经本报告核实。

#### 3.2.9 检测与排查

#### 资产排查

- [ ] 是否部署 macOS 设备并启用屏幕共享（Screen Sharing）
- [ ] 5900 端口是否暴露于互联网（公网可达性核查）
- [ ] 当前 macOS 版本是否低于 Tahoe 26.6.1 / Sequoia 15.7.9 / Sonoma 14.8.9
- [ ] 是否已安装 2026-07-27 / 2026-08-06 安全更新

#### 日志与行为排查

重点检查：

- macOS 统一日志（`log show --predicate 'process == "screensharingd"'`）中的异常认证/连接记录；
- 5900 端口连接来源（异常地域、扫描特征 IP）；
- 异常进程（挖矿程序、可疑后台进程）与 CPU 占用突增；
- 文件助手进程（`SSFileCopySender`/`SSFileCopyReceiver`）异常调用；
- 异常计划任务、启动项（LaunchAgents/LaunchDaemons）及内核扩展；
- 异常外联（矿池地址、C2 域名）与数据传输行为。

| 检测项 | 可疑特征 |
| --- | --- |
| 请求路径 | 不适用（VNC/RFB 协议，无 HTTP 路径） |
| 请求方法 | 异常 SRP/握手帧序列（认证前即发送文件操作请求） |
| 参数特征 | 认证帧长度字段异常 |
| User-Agent | 不适用 |
| 响应状态 | 未认证连接即返回已认证响应 |
| 进程行为 | 异常进程（xmrig 等挖矿程序）、高 CPU 占用 |
| 文件行为 | 文件助手进程异常复制系统文件 |
| 网络行为 | 5900 端口异常连接来源、矿池/C2 外联 |

#### 3.2.10 IOC与检测规则

#### IOC信息

| 类型 | IOC | 说明 | 可信度 |
| --- | --- | --- | --- |
| IP | 暂无公开 IOC | NCSC 未公布攻击来源 IP | 低 |
| 域名 | 暂无公开 IOC | — | 低 |
| URL | 暂无公开 IOC | — | 低 |
| SHA256 | 暂无公开 IOC | 挖矿载荷哈希未公开 | 低 |

???+ example "Sigma / YARA / Snort / Suricata规则"

    ```text
    # 暂无公开检测规则。建议：
    # 1. 对 5900 端口的互联网暴露面收敛与访问控制；
    # 2. 监控 screensharingd 进程异常行为与文件助手进程调用；
    # 3. 监控挖矿程序特征（xmrig 等）与矿池外联流量。
    ```

#### 3.2.11 风险影响

漏洞成功利用后，可能导致：

- 未经授权访问系统或业务接口；
- 远程执行任意系统命令；
- 获取应用、系统或数据库权限（root）；
- 泄露账号、密钥、配置文件及业务数据（任意文件读取）；
- 篡改网站页面、配置或业务数据；
- 植入WebShell、后门或恶意程序（挖矿程序）；
- 横向移动至其他服务器或业务系统；
- 中断业务服务或破坏系统可用性；
- 被勒索软件、挖矿木马或僵尸网络利用。

#### 3.2.12 处置优先级

| 研判维度 | 状态 |
| --- | --- |
| 互联网暴露 | 是（约 4 万台 Mac 暴露，研究者公开扫描估计） |
| 无需认证利用 | 是 |
| 利用复杂度低 | 是 |
| 已公开PoC | 是 |
| 已确认在野利用 | 是 |
| 已纳入CISA KEV | 否 |
| 影响核心业务 | 是 |
| 涉及敏感数据 | 是 |

!!! danger "综合处置优先级：P0"
    - **P0：紧急处置**，已在野利用或可直接影响核心互联网资产；
    - **P1：24小时内处置**，PoC公开且利用门槛较低；
    - **P2：3个工作日内处置**，利用条件受限或暂无成熟PoC；
    - **P3：常规整改**，风险较低或仅影响非关键场景。

#### 3.2.13 修复与缓解措施

#### 官方修复

1. 将 macOS 升级至 `Tahoe 26.6.1` / `Sequoia 15.7.9` / `Sonoma 14.8.9` 或更高版本；
2. 安装 Apple 官方安全更新（APPLE-SA-08-06-2026，support.apple.com/en-us/148170/148171/148172）；
3. 升级前完成配置、程序和业务数据备份（Time Machine）；
4. 在测试环境验证兼容性及回退方案；
5. 升级后核验版本、补丁状态及业务运行情况；
6. 重新执行漏洞检测并留存修复证据。

#### 临时缓解

无法立即升级时，可采取：

- **关闭屏幕共享功能**（系统设置 → 通用 → 共享 → 屏幕共享）或仅允许可信局域网来源；
- 通过 macOS 应用防火墙（Application Firewall）或外部防火墙限制 5900 端口仅可信 IP 可访问；
- 收敛互联网暴露面：公网禁止直接暴露 5900 端口，改用 VPN 接入；
- 启用身份认证、多因素认证和最小权限控制；
- 修改默认账号、默认密码及默认密钥；
- 加强日志审计、主机监控和异常外联检测；
- 对高风险系统实施网络隔离或访问收敛。

#### 3.2.14 修复验证

- [ ] 产品已升级至安全版本
- [ ] 官方补丁已正确安装
- [ ] 高风险接口已限制访问
- [ ] PoC或检测规则无法再次触发
- [ ] 未发现异常账号、进程、文件或计划任务
- [ ] 未发现异常网络连接或数据外传
- [ ] 业务功能运行正常
- [ ] 修复过程及验证结果已留档

#### 3.2.15 本地资产关联情况

| 项目 | 结果 |
| --- | --- |
| 是否存在相关资产 | 待核查 |
| 关联资产数量 | 不适用 |
| 互联网暴露数量 | 不适用 |
| 确认受影响数量 | 不适用 |
| 已修复数量 | 不适用 |
| 待修复数量 | 不适用 |
| 责任单位 | 不适用 |
| 完成时限 | 不适用 |
| 当前状态 | 待核查 |

> 本站在线公开文档、无内部资产台账，本地资产关联情况不适用，需各单位结合自身资产清单核查。

#### 3.2.16 参考资料

1. [Apple 安全公告（Tahoe 26.6.1）](https://support.apple.com/en-us/148172)
2. [Apple 安全公告（Sequoia 15.7.9）](https://support.apple.com/en-us/148171)
3. [Apple 安全公告（Sonoma 14.8.9）](https://support.apple.com/en-us/148170)
4. [NVD - CVE-2026-65400](https://nvd.nist.gov/vuln/detail/CVE-2026-65400)
5. [seclists Full Disclosure（技术披露）](http://seclists.org/fulldisclosure/2026/Aug/37)
6. [Huntress：macOS Screen Sharing RCE 分析](https://www.huntress.com/blog/macos-screen-sharing-rce-patched)
7. [Security Affairs：macOS Screen Sharing Flaw Exploited to Deploy Monero Miners](https://securityaffairs.com/197234/uncategorized/macos-screen-sharing-flaw-exploited-to-deploy-monero-miners.html)
8. [安全圈：macOS屏幕共享漏洞遭利用，被植入门罗币挖矿程序](https://mp.weixin.qq.com/s?__biz=MzIzMzE4NDU1OQ==&mid=2652078290&idx=1&sn=4b5441fb275403e73cf04d21dc5c6dbe)

---

### 3.3 QVD-2026-52627（GHSA-mqjf-5f49-2fjh）— GeoServer jsonArrayContains 未授权 SQL 注入漏洞（公开 PoC，已观测利用尝试）

!!! danger "严重漏洞"
    开源地理空间服务器 GeoServer 的 OGC Filters 在配合 PostGIS DataStore 执行 `jsonArrayContains` 函数时存在未授权 SQL 注入，数据库为高权限账号时可升级为远程代码执行；公开后数小时内即遭批量探测。

#### 3.3.1 基本信息

| 字段 | 内容 |
| --- | --- |
| **CVE编号** | 暂无（尚未分配 CVE；GitHub 安全通告编号 `GHSA-mqjf-5f49-2fjh`，奇安信 QVD 编号 `QVD-2026-52627`） |
| **CWE编号** | `CWE-89`（SQL Injection） |
| **漏洞名称** | GeoServer jsonArrayContains 未授权 SQL 注入漏洞 |
| **漏洞类型** | SQL 注入（未授权）→ 特定配置下远程代码执行 |
| **风险等级** | 严重 |
| **CVSS评分** | 9.8（GitHub 安全通告 GHSA-mqjf-5f49-2fjh 口径） |
| **CVSS向量** | `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H`（GHSA 口径，公开报道引用） |
| **EPSS评分** | 暂无数据（无 CVE 编号，FIRST EPSS 未收录） |
| **披露日期** | 2026-08-12（研究者 @q1uf3ng 于 X 平台公开，10:46 UTC） |
| **更新日期** | 2026-08-14（奇安信 QVD-2026-52627 通告）；修复版本随后发布 |
| **CISA KEV** | 否（截至 2026-08-15 未收录） |
| **在野利用** | 已观测利用尝试（watchTowr：公开后数小时内记录数百次探测，来自少量源 IP；未观察到后续利用动作） |
| **PoC状态** | 已公开（GitHub 仓库 mhtsec/GeoServer-jsonArrayContains-PG-RCE，Python） |
| **补丁状态** | 已发布（GeoServer 3.0.1 / 2.28.5 / 2.27.6） |
| **信息可信度** | 高（GeoServer 项目维护者公告 + watchTowr/THN/奇安信交叉印证） |

#### 3.3.2 漏洞摘要

GeoServer 是广泛部署于政府、企业与科研机构的开源地理空间数据服务器。该零日漏洞位于 **OGC Filters 配合 PostGIS DataStore 实现的 `jsonArrayContains` 函数**：GeoServer 项目维护者确认"当执行 OGC Filters 且使用 PostGIS DataStore 实现时，`jsonArrayContains` 函数存在 SQL 注入漏洞"，要求 **PostGIS 12 及以上版本且数据列为 String 或 JSON 字段**；对于 PostGIS 12 及以上，`jsonArrayContains(<column>, <pointer>, <value>)` 函数将 `<value>` **未转义直接写入生成的 SQL**。

未认证攻击者可借此注入 SQL 语句；在数据库账号为高权限（如系统管理员 sa）的配置下，可自然升级为远程代码执行。该漏洞为 2023 年 `CVE-2023-25158`（CVSS 9.8）的**回归**。受影响 Maven 包为 `org.geotools:gt-jdbc-postgis`。

研究者 @q1uf3ng 于 2026-08-12 10:46 UTC 在 X 平台公开漏洞；watchTowr 在公开后数小时内即观测到数百次探测尝试（触发错误响应、比对响应以建立漏洞清单），主要来自少量源 IP；随后奇安信 CERT 于 2026-08-14 发布 QVD-2026-52627 "已复现"通告。

> 披露当天（2026-08-12）即为"公开即被探测"的典型：攻击者无需等待成熟利用代码，仅凭公开技术细节即可批量扫描比对响应。

#### 3.3.3 影响产品与版本

| 厂商 | 产品/组件 | 受影响版本 | 修复版本 | 部署形态 |
|---|---|---|---|---|
| GeoServer 项目 | GeoServer（依赖 `org.geotools:gt-jdbc-postgis`） | `3.0.1` 之前、`2.28.5` 之前、`2.27.6` 之前 | `3.0.1`、`2.28.5`、`2.27.6` | 本地/容器/云服务器 |
| GeoServer 项目 | PostGIS DataStore（配合 PostGIS 12+，String/JSON 字段） | 使用 `jsonArrayContains` 过滤器的环境 | 升级 GeoServer 至修复版本 | 本地/容器 |

!!! warning "版本说明"
    版本范围以 GeoServer 项目维护者公告（GHSA-mqjf-5f49-2fjh）为准；具体受影响的 `gt-jdbc-postgis` Maven 版本范围详见 GitHub 安全通告。奇安信通告（QVD-2026-52627）发布时称"暂无补丁"，后续 GeoServer 已发布 3.0.1/2.28.5/2.27.6 修复版本，本报告以最新修复信息为准。

#### 3.3.4 漏洞分类

| 分类体系 | 编号 | 名称 |
| --- | --- | --- |
| CWE | `CWE-89` | SQL 注入（SQL Injection） |
| OWASP | `A03:2021` | 注入（Injection） |
| CAPEC | `CAPEC-66` | SQL 注入（SQL Injection） |

#### 3.3.5 利用条件与技术原理

| 条件 | 状态 | 说明 |
| --- | --- | --- |
| 网络可达 | 是 | 可访问 GeoServer OGC 服务端点（WFS/WMS 等） |
| 身份认证 | 无需 | 未授权即可触发过滤器执行 |
| 用户交互 | 不需要 | 无需点击链接或打开文件 |
| 特定配置 | 是 | PostGIS 12+ DataStore 且字段为 String/JSON 类型；数据库账号高权限时可 RCE |
| 利用复杂度 | 低 | 构造含 `jsonArrayContains` 的 OGC Filter 请求即可 |

**技术原理：**

`jsonArrayContains(<column>, <pointer>, <value>)` 在 PostGIS 12 及以上版本将 `<value>` 参数**未转义直接拼接**进生成的 SQL 语句，构成 SQL 注入。该缺陷是 2023 年 `CVE-2023-25158` 的回归。攻击者通过 OGC 过滤表达式（Filter Encoding）构造恶意 `<value>` 载荷，在数据库账号具备高权限（如 sa）时可进一步实现远程代码执行。

**核心利用链：**

```text
攻击者
  └─ 发现暴露的 GeoServer OGC 服务（WFS/WMS 等）
      └─ 构造含 jsonArrayContains 过滤器的请求，注入恶意 SQL 值
          └─ 触发 PostGIS DataStore 未转义拼接 <value>
              └─ 注入 SQL 语句执行（数据库 sa/高权限账号）
                  └─ 特定配置下实现远程代码执行
```

#### 3.3.6 MITRE ATT&CK 映射

| 战术 | 技术编号 | 技术名称 | 关联说明 |
| --- | --- | --- | --- |
| Initial Access | `T1190` | Exploit Public-Facing Application | 利用互联网暴露的 GeoServer OGC 服务获取初始访问 |

> 仅保留与实际利用场景相关的 ATT&CK 技术，不机械填充完整攻击链。

#### 3.3.7 PoC与利用情况

| 字段 | 内容 |
| --- | --- |
| **PoC状态** | 已公开（GitHub 仓库含 SQL 注入/RCE 利用链；另有第三方应急补丁脚本） |
| **PoC来源** | GitHub |
| **PoC地址** | https://github.com/mhtsec/GeoServer-jsonArrayContains-PG-RCE （另有 https://github.com/ubitquity/UBITQUITY-GeoServer-Security-Patch 应急修复补丁） |
| **作者/组织** | mhtsec（PoC，11 stars）；ubitquity（应急补丁，1 star） |
| **发布日期** | 2026-08-13（mhtsec PoC 仓库创建时间） |
| **支持版本** | 未打修复补丁的 GeoServer（< 3.0.1 / < 2.28.5 / < 2.27.6），需 PostGIS DataStore |
| **验证方式** | 无害化验证：仅通过公开版本信息核对补丁状态（如 `GetCapabilities` 响应中的版本/服务信息）；PoC 能力为未授权 SQL 注入与特定配置下 RCE |
| **代码可信度** | 中（mhtsec 仓库 11 stars，Python 实现，公开数日内创建；未审计，未发现可疑二进制） |
| **安全审计情况** | 未审计（mhtsec）；ubitquity 仓库为防御性应急补丁，未发现异常 |

**PoC能力：**

- [ ] 仅识别版本
- [x] 可无害验证漏洞
- [x] 可读取目标信息（SQL 注入探测）
- [x] 可执行命令（数据库 sa 配置下）
- [ ] 可获取目标权限
- [ ] 存在破坏性或持久化行为
- [ ] 存在可疑外联或供应链风险

???+ warning "PoC代码或验证请求"

    公开 PoC（mhtsec）包含完整未授权 SQL 注入与 RCE 链路，属攻击性载荷，本报告**不嵌入**。以下仅为**无害化验证**示例（版本/服务信息核对，不触发漏洞）：

    ```http
    # 无害化版本核查：获取 GeoServer 服务能力信息（不触发注入）
    GET /geoserver/wfs?SERVICE=WFS&VERSION=1.1.1&REQUEST=GetCapabilities HTTP/1.1
    Host: <target>
    Accept: application/xml

    # 通过响应中的版本信息判断是否已升级至 3.0.1 / 2.28.5 / 2.27.6
    ```

    漏洞触发点为 OGC Filter 中 `jsonArrayContains(<column>, <pointer>, <value>)` 的 `<value>` 未转义拼接（PostGIS 12+，String/JSON 字段），具体注入载荷见 [mhtsec PoC 仓库](https://github.com/mhtsec/GeoServer-jsonArrayContains-PG-RCE)（仅作防御研究参考）。禁止对未授权目标执行任何验证。

#### 3.3.8 暴露面与资产检索

| 类型 | 检索特征 |
| --- | --- |
| HTTP标题 | `title="GeoServer"`（公开语法示例） |
| 响应头 | `Server: GeoServer`（公开语法示例） |
| 页面特征 | `body="GeoServer"`（公开语法示例） |
| Favicon Hash | `icon_hash="97540678"`（公开流传的 GeoServer favicon 哈希示例，源自历史 CVE-2023-25157 检索语法） |
| 默认端口 | `8080`（默认 Tomcat 端口）/ `80` |
| FOFA | `app="GeoServer"`（公开语法示例） |
| Hunter | `web.title="GeoServer"`（公开语法示例） |
| Shodan | `http.title:"GeoServer"`（公开语法示例） |
| ZoomEye | `app:"GeoServer"`（公开语法示例） |

> **未执行任何查询。** 本栏目仅列出公开已知的检索语法示例，严禁对目标执行主动扫描或调用测绘平台。

#### 3.3.9 检测与排查

#### 资产排查

- [ ] 是否部署受影响产品或组件（GeoServer / GeoTools）
- [ ] 当前版本是否处于影响范围（< 3.0.1 / < 2.28.5 / < 2.27.6）
- [ ] 是否使用 PostGIS DataStore（PostGIS 12+）且含 String/JSON 字段
- [ ] 服务或管理接口是否暴露互联网
- [ ] 数据库账号是否为高权限（sa/超级用户）
- [ ] 是否已安装官方补丁或采取缓解措施

#### 日志与行为排查

重点检查：

- GeoServer 访问日志（WFS/WMS 请求中的异常过滤表达式）；
- WAF、防火墙、IDS/IPS及态势感知告警；
- 数据库查询日志中的异常 SQL 语句；
- Web服务异常拉起Shell、PowerShell、curl、wget等进程；
- 临时目录、Web目录和启动目录中的可疑文件；
- 异常DNS解析、外联IP、反向连接及数据传输行为。

| 检测项 | 可疑特征 |
| --- | --- |
| 请求路径 | `/geoserver/wfs`、`/geoserver/ows` 等 OGC 端点 |
| 请求方法 | `GET/POST`（含 Filter 编码参数） |
| 参数特征 | 过滤表达式含 `jsonArrayContains`、SQL 关键字或编码混淆载荷 |
| User-Agent | 扫描器或自动化工具特征 |
| 响应状态 | 触发 SQL 错误导致的异常响应（500/错误信息） |
| 进程行为 | Web服务拉起命令解释器或下载工具 |
| 文件行为 | 写入临时目录、Web目录或启动目录 |
| 网络行为 | 异常DNS、恶意外联或反向连接 |

#### 3.3.10 IOC与检测规则

#### IOC信息

| 类型 | IOC | 说明 | 可信度 |
| --- | --- | --- | --- |
| IP | 暂无公开 IOC | watchTowr 观测到探测来自少量源 IP，未公开具体地址 | 低 |
| 域名 | 暂无公开 IOC | — | 低 |
| URL | `/geoserver/wfs` 等 OGC 端点 | 被利用端点（非攻击者 IOC，属排查特征） | 高 |
| SHA256 | 暂无公开 IOC | — | 低 |

???+ example "Sigma / YARA / Snort / Suricata规则"

    ```text
    # 暂无公开检测规则。建议在 WAF/IDS 侧对以下特征进行告警：
    # 1. OGC 请求（wfs/ows/wms）中的 jsonArrayContains 过滤表达式；
    # 2. 过滤参数中出现 SQL 关键字（UNION/SELECT/CAST 等）或报错注入特征；
    # 3. 短时间内来自同一源 IP 的大量 OGC 探测请求。
    ```

#### 3.3.11 风险影响

漏洞成功利用后，可能导致：

- 未经授权访问系统或业务接口；
- 远程执行任意系统命令（数据库高权限配置下）；
- 获取应用、系统或数据库权限；
- 泄露账号、密钥、配置文件及业务数据（地理空间数据）；
- 篡改网站页面、配置或业务数据；
- 植入WebShell、后门或恶意程序；
- 横向移动至其他服务器或业务系统；
- 中断业务服务或破坏系统可用性；
- 被勒索软件、挖矿木马或僵尸网络利用。

#### 3.3.12 处置优先级

| 研判维度 | 状态 |
| --- | --- |
| 互联网暴露 | 是 |
| 无需认证利用 | 是 |
| 利用复杂度低 | 是 |
| 已公开PoC | 是 |
| 已确认在野利用 | 否（已观测利用尝试/探测） |
| 已纳入CISA KEV | 否 |
| 影响核心业务 | 是 |
| 涉及敏感数据 | 是 |

!!! danger "综合处置优先级：P1"
    - **P0：紧急处置**，已在野利用或可直接影响核心互联网资产；
    - **P1：24小时内处置**，PoC公开且利用门槛较低；
    - **P2：3个工作日内处置**，利用条件受限或暂无成熟PoC；
    - **P3：常规整改**，风险较低或仅影响非关键场景。

#### 3.3.13 修复与缓解措施

#### 官方修复

1. 将 GeoServer 升级至 `3.0.1` / `2.28.5` / `2.27.6` 或更高版本（同时升级 `org.geotools:gt-jdbc-postgis` 依赖）；
2. 安装官方安全补丁/修复版本；
3. 升级前完成配置、程序和业务数据备份；
4. 在测试环境验证兼容性及回退方案；
5. 升级后核验版本、补丁状态及业务运行情况；
6. 重新执行漏洞检测并留存修复证据。

#### 临时缓解

无法立即升级时，可采取：

- 限制 GeoServer OGC 服务（WFS/WMS/OWS）的互联网访问，仅允许可信来源；
- 通过防火墙、ACL、安全组仅允许可信IP访问；
- 在WAF、IPS或网关增加针对 `jsonArrayContains` 过滤表达式的拦截规则；
- 将数据库账号权限收敛为最小权限（避免 sa/超级用户直连）；
- 对高风险系统实施网络隔离或访问收敛；
- 关闭或禁用不受业务必需的 OGC 服务端点；
- 加强日志审计、主机监控和异常外联检测。

#### 3.3.14 修复验证

- [ ] 产品已升级至安全版本
- [ ] 官方补丁已正确安装
- [ ] 高风险接口已限制访问
- [ ] PoC或检测规则无法再次触发
- [ ] 未发现异常账号、进程、文件或计划任务
- [ ] 未发现异常网络连接或数据外传
- [ ] 业务功能运行正常
- [ ] 修复过程及验证结果已留档

#### 3.3.15 本地资产关联情况

| 项目 | 结果 |
| --- | --- |
| 是否存在相关资产 | 待核查 |
| 关联资产数量 | 不适用 |
| 互联网暴露数量 | 不适用 |
| 确认受影响数量 | 不适用 |
| 已修复数量 | 不适用 |
| 待修复数量 | 不适用 |
| 责任单位 | 不适用 |
| 完成时限 | 不适用 |
| 当前状态 | 待核查 |

> 本站在线公开文档、无内部资产台账，本地资产关联情况不适用，需各单位结合自身资产清单核查。

#### 3.3.16 参考资料

1. [GitHub Security Advisory GHSA-mqjf-5f49-2fjh](https://github.com/geoserver/geoserver/security/advisories/GHSA-mqjf-5f49-2fjh)
2. [mhtsec PoC 仓库：GeoServer-jsonArrayContains-PG-RCE](https://github.com/mhtsec/GeoServer-jsonArrayContains-PG-RCE)
3. [ubitquity 应急修复补丁](https://github.com/ubitquity/UBITQUITY-GeoServer-Security-Patch)
4. [Security Affairs：GeoServer Zero-Day Is Already Being Probed](https://securityaffairs.com/197216/hacking/geoserver-zero-day-is-already-being-probed-thats-the-problem.html)
5. [奇安信 QVD-2026-52627 通告（公开报道转载）](https://mp.weixin.qq.com/s?__biz=MzIzMzE4NDU1OQ==&mid=2652078290&idx=1&sn=4b5441fb275403e73cf04d21dc5c6dbe)
6. [CVE-2023-25158（历史回归源）](https://nvd.nist.gov/vuln/detail/CVE-2023-25158)

---

## 四、其他漏洞简报

### 黑鸟 — 12KB 后门将 C2 信息藏匿于空白空格（恶意软件分析，无 CVE）

| 字段 | 内容 |
| --- | --- |
| 风险等级 | 中危（威胁情报，无 CVE） |
| CVSS评分 | 不适用 |
| CWE编号 | 不适用 |
| 影响产品 | Windows 系统用户（窃密类后门） |
| 影响版本 | 不适用 |
| PoC状态 | 不适用 |
| 在野利用 | 是（攻击者分发中） |
| 修复版本 | 不适用（防范为主） |

**漏洞描述：**  
黑鸟披露一款约 **12KB** 的窃密后门样本：其配置文件在记事本中显示为一片空白，C2 服务器域名被藏匿于**空白空格**之中（利用不可见字符/空白编码隐蔽传输），打开即见空白的系统配置文件内实际包含黑客远程控制服务器的完整域名，属"肉眼看不见的窃密通道"。此类小型化、隐蔽编码后门难以被常规静态查杀与人工审计发现。来源：[黑鸟 - 肉眼看不见的窃密通道](https://mp.weixin.qq.com/s?__biz=MzAxOTM1MDQ1NA==&mid=2451188103&idx=1&sn=b4c0fe5ff7fb1c189608de7406e80a5f)。

**修复建议：**  
加强终端防护与 EDR 覆盖，重点关注异常配置文件与不可见字符；监控可疑进程外联与 DNS 解析；对来源不明的配置文件/脚本进行内容审计（含空白字符检测）；及时更新恶意软件特征库。

---

### NIST 求助 AI 管理漏洞库 — AI 使漏洞数量暴增 72%（行业动态，无 CVE）

| 字段 | 内容 |
| --- | --- |
| 风险等级 | 中危（行业动态，无 CVE） |
| CVSS评分 | 不适用 |
| CWE编号 | 不适用 |
| 影响产品 | 漏洞管理生态（NVD 及依赖 CVE/NVD 的组织） |
| 影响版本 | 不适用 |
| PoC状态 | 不适用 |
| 在野利用 | 不适用 |
| 修复版本 | 不适用 |

**漏洞描述：**  
AI 正在加速漏洞发现，却也使漏洞数量急剧膨胀——相关报道指出漏洞数量暴增 **72%**，NIST 正寻求借助 AI 手段管理其漏洞数据库（NVD）。对依赖 NVD/CVE 数据进行漏洞管理的企业而言，漏洞数量激增与人工分析瓶颈将加大漏洞优先级研判难度，需引入自动化分级与上下文关联能力。来源：[安全圈 - AI让漏洞暴增72%，NIST求助AI管理漏洞库](https://mp.weixin.qq.com/s?__biz=MzIzMzE4NDU1OQ==&mid=2652078290&idx=2&sn=59601ed506d26630b5fd959967127d4d)。

**修复建议：**  
企业漏洞管理流程建议引入 CVSS/EPSS/KEV 等多维自动化排序，缓解漏洞洪峰下的研判压力；关注 NVD 数据质量变化对内部漏洞库的影响。

---

## 五、当日处置建议

1. **P0 优先处置 `CVE-2026-58231`（SAP Commerce Cloud）**：立即核查 Commerce Cloud 环境版本（Data Hub Adapter），未达 `2211.55` / `2211-jdk21.17` 的按暴露风险处理；升级前按 SAP Note 3771065 配置 IP Filter Set 收敛 `/datahubadapter/import/**` 端点访问，并排查异常导入请求与利用痕迹；
2. **P0 优先处置 `CVE-2026-65400`（macOS Screen Sharing）**：立即核查互联网暴露 5900 端口的 Mac 设备并升级至 Tahoe 26.6.1 / Sequoia 15.7.9 / Sonoma 14.8.9；无法升级的先关闭屏幕共享或限制 5900 端口访问；排查已植入门罗币挖矿程序的主机（异常进程、高 CPU、矿池外联）；
3. **P1 处置 `QVD-2026-52627`（GeoServer）**：升级至 `3.0.1` / `2.28.5` / `2.27.6`；收敛 OGC 服务暴露面，WAF 增加 `jsonArrayContains` 过滤表达式拦截规则，收敛数据库账号权限；
4. 对疑似遭受攻击的系统保留日志、流量、进程、文件及内存证据；
5. 对发现异常的主机开展WebShell、恶意进程、异常账号及横向移动排查；
6. 完成修复后进行复测，并更新资产台账和漏洞处置记录。

---

## 六、风险跟踪台账

| 序号 | CVE编号 | 关联资产 | 责任单位 | 等级 | 优先级 | 当前状态 | 完成时限 | 备注 |
|---|---|---|---|---|---|---|---|---|
| 1 | `CVE-2026-58231` | 待核查（本单位 SAP Commerce Cloud 实例） | 待核查 | 严重 | P0 | 整改中 | 待核查 | 在野利用已确认；需升级至 2211.55 / 2211-jdk21.17 |
| 2 | `CVE-2026-65400` | 待核查（本单位 macOS 设备） | 待核查 | 严重 | P0 | 整改中 | 待核查 | 在野利用已确认；需升级至 Tahoe 26.6.1 / Sequoia 15.7.9 / Sonoma 14.8.9 |
| 3 | `QVD-2026-52627`（GHSA-mqjf-5f49-2fjh） | 待核查（本单位 GeoServer 实例） | 待核查 | 严重 | P1 | 整改中 | 待核查 | 暂无 CVE 编号；需升级至 3.0.1 / 2.28.5 / 2.27.6 |

> 注：本站在线公开文档、无内部资产台账，台账中"关联资产/责任单位/完成时限"均标注"待核查"，由各单位结合自身资产清单与处置流程补充。

---

> **免责声明：** 本报告基于厂商安全公告、公开漏洞数据库及威胁情报整理，仅用于网络安全防御、风险排查和授权安全测试。漏洞版本范围、PoC状态、利用情况及修复方案可能发生变化，使用前应以厂商最新公告为准。
